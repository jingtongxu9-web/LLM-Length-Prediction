"""Train a progressive remaining-length model."""


def main() -> None:
    raise SystemExit(
        "Build stride-based (features, R_t) samples and train the PLP correction module."
    )


if __name__ == "__main__":
    main()
